import { Dashboard } from "../components/dashboard/dashboard";
import { Nav } from "../components/dashboard/nav";

const Home = () => {
  return (
    <main>
      <Nav />
      <Dashboard />
    </main>
  );
};

export default Home;
